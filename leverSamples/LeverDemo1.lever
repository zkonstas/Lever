program {

    output "hello world!\n";

    var res;

    res = get "uk election",["since":"2015-05-01","until":"2015-05-07"];
    output res;

    var xAxis = ["Cameron", "Milband", "Sturgeon", "Clegg"];
    var counts = [4];
    counts[0] = result.tweetCount "cameron";
    counts[1] = result.tweetCount "milband";
    counts[2] = result.tweetCount "sturgeon";
    counts[3] = result.tweetCount "clegg";

    graph "bar", "UK Election Tweets - Popular Tweets", xAxis, counts;
}